
services:
  # Servicio del Backend (Laravel)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: unach_connect_backend
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./backend:/var/www/html
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - APP_URL=http://localhost:8000
      - DB_HOST=db.deysrerbfbsuehnwijmp.supabase.co

  # Servicio del Frontend (React + Vite)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: unach_connect_frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules # Máscara para evitar conflicto de binarios de node_modules entre host y container
    environment:
      - VITE_API_URL=http://localhost:8000/api
    depends_on:
      - backend
    networks:
      - unach_network

networks:
  unach_network:
    driver: bridge
